#ifndef BUTTON_CONFIG_H
#define BUTTON_CONFIG_H

void main_window_config_provider(ClickConfig **, Window*);
void splash_window_config_provider(ClickConfig **, Window*);
void error_window_config_provider(ClickConfig **, Window*);

#endif